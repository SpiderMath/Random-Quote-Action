name: main
on: [push]
  # schedule:
  #   # - cron: '*/1 * * * *'
    # Every 30 minutes should be enough

jobs:
  check-npm-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm -v